import './ContactsPage.css';

export function ContactsPage() {
  return (
    <section className="container contacts-page">
      <p className="contacts-page__eyebrow">Contacts</p>
      <h1 className="contacts-page__title">Контакты</h1>
      <p className="contacts-page__text">
        Позже добавим ссылки, способы связи и, если понадобится, форму.
      </p>
    </section>
  );
}
